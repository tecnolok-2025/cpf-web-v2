version: "3.9"
services:
  cpf:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
    environment:
      - OPENAI_MODEL=gpt-4o-mini
      # Pegá tu key si querés IA completa (moderación + mejora + consultas):
      # - OPENAI_API_KEY=TU_API_KEY